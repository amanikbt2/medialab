# 🚀 Smart Insert - Quick Reference

## What Changed?

Your insert command is now **intelligent and forgiving**. You can type naturally, with typos and casual English - the system will understand and execute!

## Try These Right Now

### New Element Types (Not in old insert system)

```
insert slider
insert toggle
insert spinner (loading animation)
insert modal
insert badge
insert alert
insert checkbox
insert radio
insert search
insert menu
insert navbar
```

### Casual English (All work identically)

```
insert button
add button
create button
make button
give me button
put button
show me button
please button
can you insert button
could you add button
```

### With Misspellings (Auto-corrected)

```
"give me a buton" → inserts button
"spinnin circle" → inserts spinner
"modl with blue" → inserts blue modal
"checkbx" → inserts checkbox
"navbr plz" → inserts navbar
```

### With Styling

```
insert glowing cyan button
insert large purple modal
insert neon green toggle
insert blue slider
insert red alert
insert animated spinner
insert gradient button
insert rounded card
```

## How It Works (In 3 Steps)

1. **You type naturally** (with typos, casual English, various phrasing)
2. **System normalizes** (corrects typos, extracts intent)
3. **Element appears** (instantly on canvas!)

## Element Quick Access

### Interactive Controls

| Command           | Result            |
| ----------------- | ----------------- |
| `insert slider`   | Range selection   |
| `insert toggle`   | On/off switch     |
| `insert spinner`  | Loading animation |
| `insert checkbox` | Checkable box     |
| `insert radio`    | Radio button      |
| `insert search`   | Search input      |

### Containers

| Command          | Result            |
| ---------------- | ----------------- |
| `insert modal`   | Dialog box        |
| `insert menu`    | Navigation menu   |
| `insert navbar`  | Top navigation    |
| `insert card`    | Card container    |
| `insert panel`   | Side panel        |
| `insert section` | Section container |

### UI Components

| Command             | Result             |
| ------------------- | ------------------ |
| `insert badge`      | Label/badge        |
| `insert alert`      | Alert notification |
| `insert tooltip`    | Tooltip element    |
| `insert breadcrumb` | Breadcrumb nav     |
| `insert pagination` | Page numbers       |

### Basic

| Command         | Result           |
| --------------- | ---------------- |
| `insert button` | Clickable button |
| `insert text`   | Text content     |
| `insert image`  | Image element    |
| `insert input`  | Input field      |
| `insert circle` | Circle shape     |

## Real-World Examples

### Examples That Work

```
User: "put a black slider"
→ Black slider inserted

User: "add a glowing cyan button plz"
→ Glowing cyan button inserted

User: "give me a large red alert"
→ Large red alert box inserted

User: "create a spinning loader"
→ Animated spinner inserted

User: "show me a purple toggle"
→ Purple toggle switch inserted

User: "build a neon green modal"
→ Neon green modal dialog inserted

User: "insert checkbox with border"
→ Bordered checkbox inserted

User: "add a gradient button"
→ Gradient button inserted

User: "create an animated badge"
→ Animated badge inserted
```

## Super Casual Examples (Still Work!)

```
"button plz"                    ✓ Button inserted
"gimme slider"                  ✓ Slider inserted
"put modal kthx"                ✓ Modal inserted
"can you mayb add a toggle"     ✓ Toggle inserted
"show spinner thnx"             ✓ Spinner inserted
"build alert with blue k?"      ✓ Blue alert inserted
"create badge that says hi"     ✓ Badge with text inserted
```

## Styling Keywords

### Colors

Any color name works: red, blue, cyan, pink, purple, orange, yellow, green, etc.

### Effects

- `glow` - Glowing effect
- `neon` - Neon glow
- `shadow` - Shadow effect
- `border` - Outlined
- `gradient` - Color gradient
- `blur` - Frosted glass
- `transparent` - See-through

### Sizes

- tiny, small, medium, large, huge, xl, 2xl, 3xl

### Animations

- `spinning` - Rotation animation
- `bouncing` - Bounce animation
- `pulsing` - Pulse animation
- `floating` - Float animation
- `animated` - General animation

## The Forgiving System

| You type   | System understands |
| ---------- | ------------------ |
| buton      | button             |
| slidelr    | slider             |
| spinnin    | spinner            |
| modl       | modal              |
| tabel      | table              |
| checkbx    | checkbox           |
| spellz bad | Tries best match   |

## Performance

- ⚡ **Instant** for simple commands
- ⚡ **Fast** for styled commands
- ⚡ **No waiting** for preprocessing
- ⚡ **Graceful fallback** to normal chat if confused

## Multi-Element Commands

For complex layouts, you can still use natural language:

```
"create a hero section with a big title, a slider below it,
and two buttons at the bottom"
→ AI understands → Multiple elements inserted
```

## Error Recovery

If the system can't understand:

- ✓ It tries alternative interpretations
- ✓ Falls back to normal chat
- ✓ Never fails silently
- ✓ Always helpful response

## Pro Tips

1. **Combine effects**: `"glowing neon cyan spinning button"`
2. **Be specific**: `"large purple modal with content"` vs `"modal"`
3. **Stack keywords**: `"blue glowing rounded bordered button"`
4. **Type naturally**: System handles typos and casual English

## Comparison: Before vs After

### Before

```
✗ "give me button" → "I don't understand"
✗ "buton plz" → "Not recognized"
✗ "add spinner" → "Add what?"
✗ "put a modl here" → "Not supported"
```

### After

```
✓ "give me button" → Button inserted
✓ "buton plz" → Button inserted (corrected)
✓ "add spinner" → Spinner inserted
✓ "put a modl here" → Modal inserted (corrected)
```

---

## Ready to Go!

Start using the smart insert now. Type however feels natural - the system will understand! 🎨

Examples to try right now:

1. `insert glowing button`
2. `add a spinner`
3. `create modal with blue`
4. `put slider plz`
5. `give me a neon toggle`

All work perfectly! ✨
